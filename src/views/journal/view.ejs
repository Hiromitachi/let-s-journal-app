 <!DOCTYPE html>
<html>
<head>
  <title>View Journal Entry</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Special+Elite&display=swap');
    body {
      font-family: 'Special Elite', monospace, cursive;
      background-color: #6b7bba;
      margin: 0;
      padding: 40px;
      color: #000;
      position: relative;
      min-height: 100vh;
      overflow-x: hidden;
      display: flex;
      justify-content: center;
      align-items: flex-start;
    }
    .home-button {
      position: fixed;
      top: 20px;
      right: 20px;
      background: #f4c2c2;
      border-radius: 50%;
      width: 50px;
      height: 50px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      display: flex;
      justify-content: center;
      align-items: center;
      cursor: pointer;
      transition: background-color 0.3s ease;
      text-decoration: none;
      z-index: 10;
    }
    .home-button:hover {
      background-color: #f67280;
    }
    .home-button svg {
      width: 28px;
      height: 28px;
      fill: #333;
    }
    .journal-entry-label {
      position: fixed;
      top: 20px;
      left: 20px;
      background: #f4a9d1;
      padding: 10px 25px;
      border-radius: 0 10px 10px 0;
      font-weight: 700;
      color: black;
      font-size: 18px;
      user-select: none;
      clip-path: polygon(0 0, 90% 0, 100% 50%, 90% 100%, 0 100%);
      box-shadow: 0 3px 6px rgba(0,0,0,0.2);
      z-index: 10;
    }
    .back-label {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background: #28a745;
      padding: 10px 20px;
      border-radius: 8px;
      font-weight: 700;
      color: white;
      font-size: 16px;
      user-select: none;
      cursor: pointer;
      text-decoration: none;
      box-shadow: 0 4px 8px rgba(0,0,0,0.15);
      z-index: 10;
      display: flex;
      justify-content: center;
      align-items: center;
      transition: background-color 0.3s ease;
    }
    .back-label:hover {
      background-color: #218838;
      color: white;
      text-decoration: none;
    }
    .container {
      background: white;
      width: 600px;
      height: 600px;
      margin: 60px 0 0 0;
      border-radius: 12px;
      box-shadow: 10px 10px 20px rgba(0,0,0,0.3);
      position: relative;
      padding: 40px 40px 60px 40px;
      background-image:
        linear-gradient(to right, #f4a89c 1px, transparent 1px),
        linear-gradient(to bottom, #f4a89c 1px, transparent 1px);
      background-size: 30px 30px;
      font-size: 18px;
      color: #000;
      font-family: 'Special Elite', monospace, cursive;
    }
    .title-label {
      position: absolute;
      top: -40px;
      left: 50%;
      transform: translateX(-50%);
      background: #f8a89c;
      padding: 10px 60px;
      font-weight: 700;
      font-size: 28px;
      color: #000;
      border-radius: 10px 10px 0 0;
      font-family: 'Special Elite', monospace, cursive;
      user-select: none;
      letter-spacing: 3px;
      font-weight: bold;
      font-variant: small-caps;
      box-shadow: 0 3px 6px rgba(0,0,0,0.2);
      clip-path: polygon(10% 0, 20% 50%, 10% 100%, 90% 100%, 80% 50%, 90% 0);
      text-align: center;
      border: 2px solid #f8a89c;
      background-image:
        linear-gradient(45deg, #f8a89c 25%, transparent 25%),
        linear-gradient(-45deg, #f8a89c 25%, transparent 25%),
        linear-gradient(45deg, transparent 75%, #f8a89c 75%),
        linear-gradient(-45deg, transparent 75%, #f8a89c 75%);
      background-size: 20px 20px;
      background-position: 0 0, 0 10px, 10px -10px, -10px 0px;
    }
    .journal-title {
      font-family: 'Special Elite', monospace, cursive;
      font-size: 28px;
      font-weight: bold;
      color: #6c5b7b;
      margin-top: 40px;
      margin-bottom: 20px;
      text-align: center;
    }
    .journal-content {
      font-family: 'Special Elite', monospace, cursive;
      font-size: 18px;
      line-height: 1.6;
      white-space: pre-wrap;
      color: #355c7d;
      padding: 0 20px;
      min-height: 300px;
    }
    .back-label-text {
      position: absolute;
      bottom: 20px;
      right: 60px;
      font-weight: 700;
      font-size: 18px;
      color: black;
      user-select: none;
      cursor: pointer;
      font-family: 'Special Elite', monospace, cursive;
      letter-spacing: 3px;
      font-variant: small-caps;
      text-shadow: none;
    }
    a.back-label {
      position: absolute;
      bottom: 20px;
      right: 20px;
      width: 100px;
      height: 50px;
      background: #f4a9d1;
      border-radius: 0 10px 10px 0;
      box-shadow: 0 3px 6px rgba(0,0,0,0.2);
      clip-path: polygon(10% 0, 100% 0, 90% 50%, 100% 100%, 10% 100%, 0 50%);
      text-decoration: none;
      z-index: 10;
      display: flex;
      justify-content: center;
      align-items: center;
      color: black;
      font-weight: 700;
      font-size: 18px;
      font-family: 'Special Elite', monospace, cursive;
      letter-spacing: 3px;
      font-variant: small-caps;
      user-select: none;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    a.back-label:hover {
      background-color: #f67280;
      color: white;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <a href="/" class="home-button" title="Home">
    <svg viewBox="0 0 64 64" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" focusable="false">
      <path d="M32 12l22 22h-6v18h-12v-12h-8v12h-12v-18h-6z"/>
    </svg>
  </a>
  <div class="journal-entry-label">JOURNAL ENTRY</div>
  <a href="/journal" class="back-label" aria-label="Back to Journal List">BACK</a>
  <div class="container">
    <div class="title-label"><%= journal.title %></div>
    <div class="journal-content"><%= journal.content %></div>
  </div>
  <a href="/journal" class="back-label-text">BACK</a>
</body>
</html>
