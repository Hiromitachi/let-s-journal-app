<!DOCTYPE html>
<html>
<head>
  <title>Create a New Journal Entry</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Special+Elite&display=swap');
    body {
      font-family: 'Special Elite', monospace, cursive;
      background-color: #cb68f0;
      margin: 0;
      padding: 40px;
      color: #000;
      position: relative;
      min-height: 100vh;
      overflow-x: hidden;
      display: flex;
      justify-content: center;
      align-items: flex-start;
    }
    .home-button {
      position: fixed;
      top: 20px;
      right: 20px;
      background: #f4c2c2;
      border-radius: 50%;
      width: 50px;
      height: 50px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      display: flex;
      justify-content: center;
      align-items: center;
      cursor: pointer;
      transition: background-color 0.3s ease;
      text-decoration: none;
      z-index: 10;
    }
    .home-button:hover {
      background-color: #f67280;
    }
    .home-button svg {
      width: 28px;
      height: 28px;
      fill: #333;
    }
    .journal-entry-label {
      position:fixed;
      top: 10px;
      left: 1px;
      background: #ff8cc9;
      padding: 10px 25px;
      border-radius: 0 10px 10px 0;
      font-weight: 700;
      color: black;
      font-size: 18px;
      user-select: none;
      clip-path: polygon(0 0, 90% 0, 100% 50%, 90% 100%, 0 100%);
      box-shadow: 0 3px 6px rgba(0,0,0,0.2);
      z-index: 10;
    }
    .back-label {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background: #f4a9d1;
      padding: 10px 25px;
      border-radius: 0 10px 10px 0;
      font-weight: 700;
      color: black;
      font-size: 18px;
      user-select: none;
      clip-path: polygon(10% 0, 100% 0, 90% 50%, 100% 100%, 10% 100%, 0 50%);
      cursor: pointer;
      text-decoration: none;
      box-shadow: 0 3px 6px rgba(0,0,0,0.2);
      z-index: 10;
    }
    .back-label:hover {
      background-color: #f67280;
      color: white;
      text-decoration: none;
    }
    .container {
      background: white;
      width: 600px;
      height: 600px;
      margin: 60px 0 0 0;
      border-radius: 12px;
      box-shadow: 10px 10px 20px rgba(0,0,0,0.3);
      position: relative;
      padding: 40px 40px 60px 40px;
      background-image:
        linear-gradient(to right, #f17a68 1px, transparent 1px),
        linear-gradient(to bottom, #f17e6c 1px, transparent 1px);
      background-size: 30px 30px;
      font-size: 18px;
      color: #000;
      font-family: 'Special Elite', monospace, cursive;
    }
    .title-label {
      position: absolute;
      top: -40px;
      left: 50%;
      transform: translateX(-50%);
      background: url('https://i.imgur.com/0Xq6Q0P.png') no-repeat center center;
      background-size: contain;
      padding: 10px 60px;
      font-weight: 700;
      font-size: 28px;
      color: #000;
      border-radius: 10px 10px 0 0;
      font-family: 'Special Elite', monospace, cursive;
      user-select: none;
      letter-spacing: 3px;
      font-weight: bold;
      font-variant: small-caps;
      box-shadow: 0 3px 6px rgba(0,0,0,0.2);
      clip-path: polygon(0 0, 90% 0, 100% 50%, 90% 100%, 0 100%);
      text-align: center;
    }
    form {
      margin-top: 20px;
      height: 100%;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
    }
    textarea {
      font-family: 'Special Elite', monospace, cursive;
      font-size: 18px;
      border: none;
      background: transparent;
      resize: none;
      height: 500px;
      width: 100%;
      color: #000;
      line-height: 1.5;
      outline: none;
      padding: 10px;
    }
    button {
      display: none;
    }
  </style>
</head>
<body>
  <a href="/" class="home-button" title="Home">
    <svg viewBox="0 0 64 64" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" focusable="false">
      <path d="M32 12l22 22h-6v18h-12v-12h-8v12h-12v-18h-6z"/>
    </svg>
  </a>
  <div class="journal-entry-label">JOURNAL ENTRY</div>
  <a href="/journal" class="back-label">BACK</a>
  <div class="container">
    <form action="/journal" method="POST">
      <div class="title-wrapper">
        <input type="text" id="title" name="title" class="title-tape" placeholder="ENTER TITLE" maxlength="50" required />
      </div>
      <textarea id="content" name="content" placeholder="enter journal entry...." required></textarea>
      <input type="hidden" id="mood" name="mood" value="" />
      <div class="mood-section">
        <div class="mood-label">MOOD TODAY</div>
      <div class="mood-icons">
        <button type="button" class="mood-icon" data-mood="laughing" aria-label="Laughing">😂</button>
        <button type="button" class="mood-icon" data-mood="happy" aria-label="Happy">🙂</button>
        <button type="button" class="mood-icon" data-mood="neutral" aria-label="Neutral">😐</button>
        <button type="button" class="mood-icon" data-mood="sad" aria-label="Sad">🙁</button>
      </div>
      </div>
      <button type="submit" class="submit-button">SUBMIT</button>
    </form>
  </div>
  <style>
    .title-wrapper {
      position: relative;
      margin-bottom: 20px;
      width: 100%;
      top: -40px;
    }
    .title-tape {
      display: block;
      margin: 0 auto 20px auto;
      background: hsl(335, 100%, 70%);
      font-family: 'Special Elite', monospace, cursive;
      font-weight: 700;
      font-size: 28px;
      color: #ffffff;
      border-radius: 16px;
      box-shadow: none;
      user-select: text;
      text-align: center;
      letter-spacing: 3px;
      font-variant: small-caps;
      padding: 10px 40px;
      width: 300px;
      border: none;
      outline: none;
      box-sizing: border-box;
      cursor: text;
      height: 50px;
      line-height: 50px;
      z-index: 10;
      box-shadow: none;
      position: relative;
    }
    .title-input {
      font-family: 'Special Elite', monospace, cursive;
      font-size: 24px;
      font-weight: bold;
      width: 100%;
      padding: 10px;
      border: none;
      border-bottom: 3px solid #f4a9d1;
      background: white;
      color: #ff0a0a;
      outline: none;
      box-shadow: none;
      border-radius: 0 0 12px 12px;
      user-select: text;
      text-align: center;
      position: relative;
      z-index: 1;
    }
    .title-input::placeholder {
      color: #f4a9d1;
      font-weight: normal;
    }
  </style>
  </div>
  <script>
    const moodButtons = document.querySelectorAll('.mood-icon');
    const moodInput = document.getElementById('mood');

    moodButtons.forEach(button => {
      button.addEventListener('click', () => {
        // Remove active class from all buttons
        moodButtons.forEach(btn => btn.classList.remove('active'));
        // Add active class to clicked button
        button.classList.add('active');
        // Set hidden input value
        moodInput.value = button.getAttribute('data-mood');
      });
    });
  </script>
  <style>
    .title-input {
      font-family: 'Special Elite', monospace, cursive;
      font-size: 24px;
      font-weight: bold;
      width: 100%;
      padding: 10px;
      margin-bottom: 20px;
      border: none;
      border-bottom: 3px solid #f4a9d1;
      background: url('https://i.imgur.com/0Xq6Q0P.png') no-repeat center top;
      background-size: 300px 60px;
      color: #000;
      outline: none;
      box-shadow: 0 3px 6px rgba(0,0,0,0.2);
      border-radius: 6px 6px 0 0;
      user-select: text;
      text-align: center;
    }
    .title-input::placeholder {
      color: #f4a9d1;
      font-weight: normal;
    }
    .mood-section {
      position: absolute;
      bottom: 40px;
      right: 40px;
      text-align: center;
      font-family: 'Special Elite', monospace, cursive;
      background: transparent;
      color: #000;
      box-shadow: none;
    }
    .mood-label {
      font-weight: bold;
      margin-bottom: 8px;
      color: #000;
      text-shadow: none;
      font-size: 14px;
      letter-spacing: 1px;
    }
    .mood-icons {
      display: flex;
      gap: 10px;
      justify-content: center;
    }
    .mood-icon {
      font-size: 40px;
      background: #ffffff;
      border-radius: 50%;
      border: 2px solid #f4a9d1;
      cursor: pointer;
      transition: transform 0.2s ease, background-color 0.3s ease;
      user-select: none;
      width: 50px;
      height: 50px;
      display: flex;
      justify-content: center;
      align-items: center;
      box-shadow: 0 2px 5px rgba(0,0,0,0.2);
    }
    .mood-icon {
      position: relative;
      transition: transform 0.3s ease;
      border-radius: 50%;
      overflow: visible;
      padding: 5px;
      background: transparent;
      border: none;
      cursor: pointer;
      user-select: none;
      width: 60px;
      height: 60px;
      display: flex;
      justify-content: center;
      align-items: center;
      box-shadow: none;
    }
    .mood-icon img {
      width: 48px;
      height: 48px;
      display: block;
      border-radius: 50%;
      transition: filter 0.3s ease;
    }
    .mood-icon:hover {
      transform: scale(1.3);
    }
    .mood-icon:hover::before {
      content: "";
      position: absolute;
      top: 50%;
      left: 50%;
      width: 80px;
      height: 80px;
      background: rgba(244, 169, 209, 0.6);
      border-radius: 50%;
      filter: blur(8px);
      transform: translate(-50%, -50%);
      z-index: -1;
      pointer-events: none;
      animation: shine 1.5s ease-in-out infinite alternate;
    }
    @keyframes shine {
      0% {
        filter: blur(4px);
        opacity: 0.6;
      }
      100% {
        filter: blur(12px);
        opacity: 1;
      }
    }
    .mood-icon.active {
      background-color: #f67280;
      border-color: #f67280;
      transform: scale(1.4);
    }
    .submit-button {
      margin-top: 20px;
      background-color: #2c3e50;
      color: white;
      border: none;
      border-radius: 12px;
      padding: 12px 24px;
      font-family: 'Special Elite', monospace, cursive;
      font-weight: bold;
      cursor: pointer;
      box-shadow: 0 6px 10px rgba(125, 6, 139, 0.247);
      transition: background-color 0.3s ease;
      user-select: none;
      display: block;
      width: 150px;
      text-align: center;
    }
    .submit-button:hover {
      background-color: #f67280;
      color: white;
    }
  </style>
  </div>
  <style>
    .title-input {
      font-family: 'Special Elite', monospace, cursive;
      font-size: 24px;
      font-weight: bold;
      width: 100%;
      padding: 10px;
      margin-bottom: 20px;
      border: none;
      border-bottom: 3px solid #f4a9d1;
      background: url('https://i.imgur.com/0Xq6Q0P.png') no-repeat center top;
      background-size: 300px 60px;
      color: #000;
      outline: none;
      box-shadow: 0 3px 6px rgba(0,0,0,0.2);
      border-radius: 6px 6px 0 0;
      user-select: text;
      text-align: center;
    }
    .title-input::placeholder {
      color: #f4a9d1;
      font-weight: normal;
    }
    .mood-section {
      position: absolute;
      bottom: 40px;
      right: 40px;
      text-align: center;
      font-family: 'Special Elite', monospace, cursive;
      background: transparent;
      color: #000;
      box-shadow: none;
    }
    .mood-label {
      font-weight: bold;
      margin-bottom: 8px;
      color: #000;
      text-shadow: none;
      font-size: 14px;
      letter-spacing: 1px;
    }
    .mood-icons {
      display: flex;
      gap: 10px;
      justify-content: center;
    }
    .mood-icon {
      font-size: 40px;
      background: #ffeb3b;
      border-radius: 50%;
      border: 2px solid #f4a9d1;
      cursor: pointer;
      transition: transform 0.2s ease, background-color 0.3s ease;
      user-select: none;
      width: 50px;
      height: 50px;
      display: flex;
      justify-content: center;
      align-items: center;
      box-shadow: 0 2px 5px rgba(0,0,0,0.2);
    }
    .mood-icon:hover {
      transform: scale(1.3);
      background-color: #f4a9d1;
    }
    .submit-button {
      margin-top: 20px;
      background-color: #2c3e50;
      color: white;
      border: none;
      border-radius: 12px;
      padding: 12px 24px;
      font-family: 'Special Elite', monospace, cursive;
      font-weight: bold;
      cursor: pointer;
      box-shadow: 0 6px 10px rgba(0,0,0,0.3);
      transition: background-color 0.3s ease;
      user-select: none;
      display: block;
      width: 150px;
      text-align: center;
    }
    .submit-button:hover {
      background-color: #f67280;
      color: white;
    }
  </style>
  </div>
  <style>
    .title-label {
      position: absolute;
      top: -40px;
      left: 50%;
      transform: translateX(-50%);
      background: #f4a9d1;
      padding: 10px 60px;
      font-weight: 700;
      font-size: 28px;
      color: #000;
      border-radius: 10px 10px 0 0;
      font-family: 'Special Elite', monospace, cursive;
      user-select: none;
      letter-spacing: 3px;
      font-weight: bold;
      font-variant: small-caps;
      box-shadow: 0 3px 6px rgba(0,0,0,0.2);
      clip-path: polygon(0 0, 90% 0, 100% 50%, 90% 100%, 0 100%);
      text-align: center;
      background-size: auto;
      background-repeat: no-repeat;
      background-position: center;
    }
    .mood-section {
      position: absolute;
      bottom: 40px;
      right: 40px;
      text-align: center;
      font-family: 'Special Elite', monospace, cursive;
      background: #ffeb3b;
      color: #000;
      box-shadow: 0 4px 8px rgba(0,0,0,0.2);
      border-radius: 12px;
      padding: 10px 20px;
      user-select: none;
    }
    .mood-label {
      font-weight: bold;
      margin-bottom: 8px;
      color: #000;
      text-shadow: none;
      font-size: 14px;
      letter-spacing: 1px;
    }
    .mood-icons {
      display: flex;
      gap: 10px;
      justify-content: center;
    }
    .mood-icon {
      font-size: 48px;
      background: #fff3cd;
      border: 2px solid #f4a9d1;
      border-radius: 50%;
      cursor: pointer;
      transition: transform 0.2s ease, background-color 0.3s ease;
      user-select: none;
      width: 60px;
      height: 60px;
      display: flex;
      justify-content: center;
      align-items: center;
      box-shadow: 0 3px 6px rgba(0,0,0,0.15);
    }
    .mood-icon:hover {
      transform: scale(1.3);
      background-color: #f4a9d1;
    }
    .submit-button {
      margin-top: 10px;
      background-color: #2c3e50;
      color: white;
      border: none;
      border-radius: 6px;
      padding: 8px 16px;
      font-family: 'Special Elite', monospace, cursive;
      font-weight: bold;
      cursor: pointer;
      box-shadow: 0 4px 6px rgba(0,0,0,0.3);
      transition: background-color 0.3s ease;
      user-select: none;
      display: block;
      width: 100px;
      text-align: center;
    }
    .submit-button:hover {
      background-color: #34495e;
    }
  </style>
